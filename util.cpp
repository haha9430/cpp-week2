#include <iostream>
using namespace std;

int *makeArray(int n) {
    /*
    int *array[3];

    array[0] = n / 100;
    array[1] = (n - array[0] * 100) / 10;
    array[2] = (n - array[0] * 100 - array[1] * 10);

    return array;
    */
    return 0;
}
